server:
  port: 8080

spring:
  application:
    name: web-server

  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/users, /api/users/**

